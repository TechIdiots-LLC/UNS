# Keep development-only trees out of the release archives. The release workflow uses
# `git archive`, which honours export-ignore, so without this the test suite and CI
# definitions would ship inside every uns-<version>.zip that users download.
Tests/          export-ignore
.github/        export-ignore
.gitattributes  export-ignore
